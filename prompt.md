# Reference
接口相关：（设备到传感器侧）
    （传感器+固件）侧接口参考使用：05_Software\home_assistant\Home_Assistant_C4004\Home_Assistant_C4004\README_ENTITY_API.md；
    （传感器+固件）如果接口方面有不明确的，就参考05_Software\home_assistant\Home_Assistant_C4004\Home_Assistant_C4004\dfrobot_c4004；
log相关:

参考工程：01_Reference\everything-presence-addons

项目构造参考：https://developers.home-assistant.io/docs/apps/tutorial

# 开发需求
帮我完成 区域管理界面和功能（开发完部分功能）。详细描述内容如下，其中一些内容你不确定最好跟我确定，觉得不合理的给我解决方案并且跟我确认方案。工程在这个路径下D:\User\lj\DFRobot\Project\module\C4004\C4004\05_Software\home_assistant\mmWave_addons

## 详细需求描述：
### 界面三大部分（设备总览、设备管理、区域管理）

#### 设备总览（已经开发完成，不用管，不用开发）
设备总览界面风格和界面参考，其中有红色字体做了区域标识说明了那部分什么内容或怎么修改；
参考路径为05_Software\home_assistant\mmWave_addons\resource\设备总览界面.png；
这个界面主界面顶部要展示总设备数、当前总人数、当前探测总人数、当前静止总人数；
下面展示实时设备监控矩阵，最右侧按钮全"添加设备",点击添加设备可跳转只设备管理界面；
下面就是各个设备的展示，展示二维坐标系，在坐标系中，传感器的探测方向朝上，传感器在最中间，固定坐标系的范围为x(-5-5m),y范围为（0-9m），除了显示坐标系之外，还有展示区域标签，探测范围框（这些是坐标系上要显示的内容），注意大小，因为要展示多个设备，这个大小尽可能固定，除此之外，点击这个设备，可以跳转至对应的设备数据(2.1.1)详情展示子界面设备；传感器图标：参考05_Software\home_assistant\mmWave_addons\resource\sensor.png，需要注意这是个wifi的探图案，注意它的方向代表传感器的方向；
1设备详情展示主界面（风格参考：05_Software\home_assistant\mmWave_addons\resource\设备详情展示界面.png）
顶部显示设备名称、设备id、在线状态；右侧需要三个按钮重启设备、刷新、区域配置（点击这个按钮就跳到对应得设备得区域管理中）
下方：左侧显示坐标系（这立要显示得尽量大一点合适就行）。显示大小x(-5-5m),y范围为（0-9m），注意，这个也需要展示标签区域、探测范围、；右侧为显示区：运动人数、静止人数、IO联动状态显示IO1（整体区域IO是否触发）- 显示灯    IO2 - 显示灯  IO3 - 显示灯    IO4 - 显示灯   IO5 - 显示灯 IO6- 显示灯；基础信息展示区：安装方式、实时人数上报时间、安装高度、轨迹产生米数、探测模式、轨迹存在时间、确认帧数、无人时间；
#### 设备管理（已经开发完成，不用管，不用开发）
1. 目前已经完成扫描设备，也可以扫描出设备，但是，初始化绑定功能并没有体现出来,功能已经写了,现在我需要在后端中去存储绑定的数据,存储绑定数据到devices（后端新创建一个文件devices.json（放在这个路径下/homeassistant/dfrobot_mmwave/），你需要跟我确定这其中要存什么数据？，如何绑定，例如确定了设备固件测唯一得mac地址，HA设备端唯一的设备ID,共同绑定生成一个设备号（这个设备号如何生成，删除设备后怎样处理，设备号不能太长））；这样处理后，前端就可以知道哪些设备已经是初始化绑定的，哪些是新设备未绑定，需要走绑定流程；

2. 设备显示列表，设备ID太长了，改成显示设备号

3. 扫描时，已经绑定的默认显示出来，未绑定的经过扫描后可显示出来，未绑定的不显示设备号（因为还没绑定嘛，没有设备号，可以显示个‘-’），因为没绑定，设备号没有生成

4. 设备初始化绑定流程（已经开发的弹窗，还需完善功能）
（1）.弹窗流程1：点击绑定设备。确认自动生成设备号或自定义设备号，点击下一步时，需要确定绑定号，如果自定义设备号时重复了需要提示用户；除此之外还需显示设备名称，和部署位置这两个参数，不能改变只做显示；
（2）.弹窗流程2：安装设备
安装方式：默认侧装（只做显示，无法改变）；安装角度：默认0度（只做显示，无法改变）；安装高度：（滑动条方式1.8m-2m）
（3）.弹窗流程3：探测模式
前端使用探测模式接口，后端实现具体参数逻辑
高灵敏度模式（快速响应）（前端显示模式）：确认帧数降低2帧，无人时间内部配置为5s（后端逻辑）；
静态稳定模式（持续存在）（前端显示模式）：确认帧数降低7帧，无人时间内部配置为30s（后端逻辑）；

#### 区域管理
1. 这个界面主要是为了配置区域和设备对应的一些参数。
1.1. 首先风格参考我这个D:\User\lj\DFRobot\Project\module\C4004\C4004\05_Software\home_assistant\mmWave_addons\resource\区域管理示例图.png；

1.2. 另外，这个背景是网格画布；画布支持：鼠标滚轮放大和缩小、手抓拖拉（左键拖拉）

2.接下来我描述一下功能，区域管理界面主要功能如下：
2.1 该界面左上角显示字体 区域管理界面，字体不用太大，同时左上角还要显示当前设备的设备名称，和设备号（相当于选择某一个设备进行操作，区域配置，相关参数配置等），设备号是一个可点击的下拉框，点击设备设备号之后，出现下拉框，可以切换设备号，无设备时，需要显示“无可用设备（离线设备也算 不可用设备）”；有设备时，默认是选择上一次选择的设备号，除非是从其它界面对应的设备跳转过来；

2.2 



